#pragma once
#include <iostream>
#include <string>

using namespace std;

class library
{
	public:
		addBook(Book b);
		removeBook(Book b);
		borrowBook(Member, isbn);
		returnBook(Member, isbn);
		displayBooks();

	private:


};